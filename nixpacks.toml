[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.build]
cmds = [
  "cd backend && npm install",
  "cd ../travel-planning-app && npm install && npm run build"
]

[start]
cmd = "cd backend && npm start"